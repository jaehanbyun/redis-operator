apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redis-operator.fullname" . }}-test-connection"
  labels:
    {{- include "redis-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redis-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
